# stable-ui application configuration options
# These are loaded in via the config.h header

######################
### ImGUI Settings ###
######################
HIGH_DPI_SCALE_FACTOR: 1.0
WINDOW_WIDTH: 1664
WINDOW_HEIGHT: 936

##############################
### Internal configuration ###
##############################
ENABLE_OPENGL_DEBUG_OUTPUT: 0
ENABLE_TRACE_LOGGING: 0
# Debug levels range from 1 - 5 with 1 being the most verbose and 5 being no debug logs
DEBUG_LEVEL: 2
